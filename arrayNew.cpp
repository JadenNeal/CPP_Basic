#include <iostream>
using namespace std;
int main()
{
    // double* pd = new double [3];
    // pd[0] = 0.1;
    // pd[1] = 0.2;
    // pd[2] = 0.3;

    // cout << "pd array: " << pd[0] << ", " << pd[1] << ", " << pd[2] << endl;

    // pd += 1;
    // cout << "new pd array: " << pd[0] << ", " << pd[1] << endl;

    // pd -= 1;

    // delete [] pd;

    // short room[5] = {1};
    // cout << room << endl;
    // cout << &room << endl;  // should be the whole array address

    return 0;
}