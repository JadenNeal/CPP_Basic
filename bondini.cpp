#include <iostream>
using namespace std;
int main()
{
    cout << "operation \"HyperHype\" is now activated." << endl;
    cout << "Enter your agent code:________\b\b\b\b\b\b\b\b";  // input password.
    long code;
    cin >> code;
    cout << "You entered " << code << " ..." << endl;
    cout << "Code verified!" << endl;
    
    // trivia points
    int k\u00F6rper;  // general char
    cout << "Let them eat g\u00E2teau.\n" << endl;

    // Whether a char is signed or unsigned is determined by C++
    unsigned char bar;
    signed char snark;

    // bool false if char is 0 else true
    const int MONTH = 12;  // #define MONTH 12

    // float is double by default
    float tub = 10.0 / 3.0;    // float is good to about 6 places
    float flt_a = 2.3e5f;  // float type
    double mint = 10.0 / 3.0;  // double is good to about 15 places

    return 0;
}